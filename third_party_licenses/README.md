# Vendored license provenance

`permissionless.LICENSE` is the MIT license from Pimlico's `permissionless.js` repository at the immutable `permissionless@0.4.0` tag (`e356bb2c43ce26fe3a661ec68d31315e78441c27`). The npm 0.4.0 package declares MIT but omits the license file, so the release build uses this reviewed copy when generating the exhaustive browser-bundle notice.

Source: <https://github.com/pimlicolabs/permissionless.js/blob/permissionless%400.4.0/LICENSE>
